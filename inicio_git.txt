Quick setup — if you’ve done this kind of thing before
or https://github.com/adomingor/sistema.git

We recommend every repository include a README, LICENSE, and .gitignore.
…or create a new repository on the command line

echo "# sistema" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/adomingor/sistema.git
git push -u origin master

…or push an existing repository from the command line

git remote add origin https://github.com/adomingor/sistema.git
git push -u origin master

…or import code from another repository

You can initialize this repository with code from a Subversion, Mercurial, or TFS project.

-------------------------------------------------------------- HICE ESTOS PASOS
ya existe el proyecto web.
ya existe el archivo .gitignore

ir al proyecto
cd /var/www/html/sistema
git init
git add *
git commit -m "mensaje inicial"
git remote add origin https://github.com/adomingor/sistema.git
git push -u origin master
git checkout -b domingo
